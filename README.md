# 🧁 Projeto Card de Receita

Este é um projeto simples, desenvolvido apenas com **HTML5** e **CSS3**, que recria um card de receita.

O objetivo principal foi praticar a estruturação semântica do HTML e a estilização com CSS para replicar um design específico.

## 📖 Sobre o Projeto

Este projeto é um ótimo exercício para iniciantes em desenvolvimento web. Ele foca em conceitos fundamentais de front-end:

* **HTML Semântico:** Uso correto de tags como `<main>`, `<section>`, `<h1>`, `<p>` e `<img>` para estruturar o conteúdo de forma lógica e acessível.
* **Estilização com CSS:** Aplicação de estilos para fontes (tipografia), cores, espaçamentos (padding/margin) e bordas (`border-radius`).
* **Layout:** O layout do card foi provavelmente construído usando **Flexbox** ou **CSS Grid** para centralizar e organizar os elementos.

## 🛠️ Tecnologias Utilizadas

* **HTML5**
* **CSS3**

## 🚀 Como Visualizar

Você pode visualizar este projeto:

```bash
# Clone este repositório
git clone https://github.com/nunesflavio/receita-html.git

# Entre na pasta do projeto
cd seu-repositorio

# Abra o arquivo index.html no seu navegador
